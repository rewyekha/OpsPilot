// AgentActivityStream — real-time feed of agent actions
//
// Renders the live status of all agents during an investigation.
// Subscribes to incidentStore.agentStatuses.
// Each agent card shows: name, model, status badge, tool calls, Foundry trace link.
// Cards animate state transitions: pending → running → complete / failed.
//
// Design reference: GitHub Copilot Workspace step-by-step transparency panel.
// Fluent UI components: Card, Badge, ProgressBar, Spinner, Link.

export {}  // placeholder — implementation in sprint 3

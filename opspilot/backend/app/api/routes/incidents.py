"""
Incidents router.

Endpoints:
  POST /api/incidents          — create a new incident investigation
  GET  /api/incidents          — list all incident records
  GET  /api/incidents/{id}     — retrieve a single incident with full findings
  PATCH /api/incidents/{id}    — update incident status (e.g. mark resolved)
"""
